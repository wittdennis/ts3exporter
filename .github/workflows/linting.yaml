name: go-lint

on: [pull_request]

jobs:
  lint:
    uses: wittdennis/pipelines/.github/workflows/go-lint.yaml@11429dc3cc02ef0463ad99260ddb4ec467e2a49e # 2.0.57
    with:
      go-version: "1.27.1" # renovate: datasource=github-tags depName=golang/go extractVersion=^go(?<version>.*)$ versioning=semver

name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@956a18b30b1d66ffcfacf93b3fafcaacaf6f4eda # 2.0.54
    with:
      image-name: ts3exporter
      create-artifacts: false

name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@43a933c8b6cf02c324997261163fd029dbf5cd6c # 2.0.14
    with:
      image-name: ts3exporter
      create-artifacts: false

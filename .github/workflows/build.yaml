name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@29f41b6d8146f7189dce232aa5a23ee635f7cbe0 # 2.0.2
    with:
      image-name: ts3exporter
      create-artifacts: false

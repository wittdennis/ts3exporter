name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@ec4833e22f9ab030d5c1393bc538c7fbe41885b0 # 2.0.24
    with:
      image-name: ts3exporter
      create-artifacts: false

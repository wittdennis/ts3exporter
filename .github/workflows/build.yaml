name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@debf39105aa63e76e9e5f8527f6909292a6399d3 # 2.0.35
    with:
      image-name: ts3exporter
      create-artifacts: false

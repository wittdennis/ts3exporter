name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@04ea5735969733417474a025e23b5b7e3751df07 # 2.0.52
    with:
      image-name: ts3exporter
      create-artifacts: false

name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@77479061311bbfdee843a8b8afd48297f9202a5f # 2.0.42
    with:
      image-name: ts3exporter
      create-artifacts: false

name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@749bb65f519a91aab3502c33024e62051793edda # 2.0.46
    with:
      image-name: ts3exporter
      create-artifacts: false

name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@6d890b5b6e2828cfa8c4006113fb62609802d9bd # 2.0.32
    with:
      image-name: ts3exporter
      create-artifacts: false

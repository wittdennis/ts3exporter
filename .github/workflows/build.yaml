name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@6ca7c92a984adecdeef5d074021a90c414e6f389 # 2.0.45
    with:
      image-name: ts3exporter
      create-artifacts: false

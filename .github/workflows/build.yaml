name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@75a61752f8c76c824a2629cc5ec5bb31236949ce # 2.0.15
    with:
      image-name: ts3exporter
      create-artifacts: false

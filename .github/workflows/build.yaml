name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@88c150558918ac4848717847a15cc197fee72b7f # 2.0.50
    with:
      image-name: ts3exporter
      create-artifacts: false

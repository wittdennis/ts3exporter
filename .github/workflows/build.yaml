name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@410ea280520e34a08d3f1f7f5a8e8eeb320772d1 # 2.0.26
    with:
      image-name: ts3exporter
      create-artifacts: false

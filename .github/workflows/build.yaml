name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@d1e1f201e011c692a2e9635209c0a37cabdd1d85 # 2.0.53
    with:
      image-name: ts3exporter
      create-artifacts: false

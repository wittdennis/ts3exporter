name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@7a91e18ddb5919549340a582dda28244b5de3c48 # 2.0.28
    with:
      image-name: ts3exporter
      create-artifacts: false

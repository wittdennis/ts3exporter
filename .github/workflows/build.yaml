name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@655e1bbbedb0937a5d1b5a46fdb2d0f61103faa8 # 2.0.40
    with:
      image-name: ts3exporter
      create-artifacts: false

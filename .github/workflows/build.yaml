name: pr

on: [pull_request]

jobs:
  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@06645518a72040625d2b681086324d8a266c41c0 # 2.0.47
    with:
      image-name: ts3exporter
      create-artifacts: false
